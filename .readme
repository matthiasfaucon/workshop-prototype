# Mon Projet

Ce dépôt contient le code source du projet ScanFinity, divisé en deux parties : le client et le serveur.

## Clone du Dépôt

Utilisez la commande suivante pour cloner ce dépôt :

```sh
git clone https://github.com/matthiasfaucon/workshop-prototype.git
```

## Client (/client)

Le client utilise Vue 3 avec Vite. Pour démarrer :

1. **Installation des dépendances :** `npm install`

2. **Développement :** `npm run dev`

3. **Production :** `npm run build`

## Serveur (/serveur)

Le serveur est basé sur Express. Configuration initiale :

1. **Installation des dépendances :** `npm install`

2. **Accès en lecture seule :** `node index.js`

Ouvrez [http://localhost:3000](http://localhost:3000) dans votre navigateur pour explorer l'application.